# 2026-07-02 Work Log

## Round 1 — AVF Permission Missing Auto-Degradation to QEMU Mode (later refined)

Initially implemented automatic degradation from AVF to QEMU mode in the permission detection page.
This was later refined in Round 2 based on user feedback.

## Round 2 — Simplify Permission Page to Read-Only Display + One-Time Show

Per user feedback, refined the permission detection page:

1. **Removed auto-degradation logic**: PermissionScreen no longer makes mode decisions.
   - Removed `RuntimeMode` enum, `shouldAutoDegradeToQemu`, `qemuFallbackReason`, `qemuFallbackImpact` from PermissionViewModel/PermissionState
   - Removed QemuModeStatusCard composable
   - Removed countdown/auto-proceed LaunchedEffect
   - Simplified bottom section to two cases: AVF available (green) vs AVF unavailable (info + manual button)

2. **One-time display**: Permission page now shows only once after installation.
   - Added `PERMISSION_CHECK_PASSED_KEY` boolean preference in DataStore
   - DroidvisorApp reads persisted flag at startup, skips permission page if already seen
   - Saves flag when user clicks "开始使用"

3. **VM-side detection verified**: VmManagerService already runs AvfCapabilityChecker in onCreate() and mode selection in startVm() using `_isAvfAvailable.value`. CreateVmScreen receives capabilities from VmManagerService.

### Files Modified (Round 2)
- `app/src/main/java/com/droidvisor/ui/viewmodel/PermissionViewModel.kt` — simplified, removed RuntimeMode
- `app/src/main/java/com/droidvisor/ui/screen/PermissionScreen.kt` — removed auto-degrade, simplified UI
- `app/src/main/java/com/droidvisor/MainActivity.kt` — added DataStore persistence for one-time display

### Round 3 — Remove Simulation Mode

Per user feedback, removed the SIMULATION RuntimeType and all associated code:

- **VmRuntime.kt**: Removed `SIMULATION` from RuntimeType enum
- **VmManagerService.kt**: Changed default activeRuntime to QEMU; startVm() now returns error when no runtime available (instead of simulating); removed simulateStartVm(); removed SIMULATION from stopVm() when branch; fixed hasRealRuntime to use _isAvfAvailable instead of avfBound
- **AvfCapabilityChecker.kt**: Removed `isSimulationOnly` from AvfCapabilities
- **PermissionViewModel.kt**: Replaced `isSimulationOnly` with `hasNoRuntime`
- **PermissionScreen.kt**: Changed simulation branch to "no runtime available" error state (red, Cancel icon, no start button)
- **VmManagementScreen.kt**: Renamed `AvfSimulationModeBanner` → `AvfRuntimeModeBanner`, removed simulation fallback text
- **SimulationModeBanner.kt**: Deleted
- **DockerDashboardScreen.kt**: Removed SimulationModeBanner import
- **TerminalScreen.kt**: Replaced SimulationModeBanner with inline warning card; added missing imports
- **DockerDashboardViewModel.kt**: Removed simulatePullProgress(), replaced with error state
- **VsockService.kt**: Removed SimulationVsockChannel class; createVsockChannel() returns null instead of simulation fallback
- **VirtualMachineManagerService.kt**: Updated log messages removing "simulation mode" references
