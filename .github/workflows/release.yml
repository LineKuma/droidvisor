name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 1.0.0)'
        required: true
        type: string

timeout-minutes: 30

env:
  VERSION_NAME: ${{ github.event.inputs.version || github.ref_name }}

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Android licenses
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Lint
        run: ./gradlew lintDebug

      - name: Run Unit Tests
        run: ./gradlew testDebugUnitTest

  build-release:
    name: Build Release APK
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Android licenses
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set version name
        run: |
          TAG_VERSION=$VERSION_NAME
          if [[ $TAG_VERSION == v* ]]; then
            TAG_VERSION=${TAG_VERSION:1}
          fi
          sed -i "s/versionName \".*\"/versionName \"$TAG_VERSION\"/" app/build.gradle

      - name: Build Release APK
        run: ./gradlew assembleRelease

      - name: Rename APK
        run: |
          TAG_VERSION=$VERSION_NAME
          if [[ $TAG_VERSION == v* ]]; then
            TAG_VERSION=${TAG_VERSION:1}
          fi
          cp app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/droidvisor-$TAG_VERSION.apk

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Rename Debug APK
        run: |
          TAG_VERSION=$VERSION_NAME
          if [[ $TAG_VERSION == v* ]]; then
            TAG_VERSION=${TAG_VERSION:1}
          fi
          cp app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/droidvisor-$TAG_VERSION-debug.apk

      - name: Upload Release APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: app/build/outputs/apk/release/droidvisor-*.apk
          retention-days: 30

      - name: Upload Debug APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/droidvisor-*-debug.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/') || github.event.inputs.version
        with:
          files: |
            app/build/outputs/apk/release/droidvisor-*.apk
            app/build/outputs/apk/debug/droidvisor-*-debug.apk
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}